typedef struct node{
    int data;
    struct node* next;
}node;

node* invert(node* head){
    node* lead=head->next;
    node* middle = NULL;
    node* lag=head;
    while(middle){

    }
    return head;
}